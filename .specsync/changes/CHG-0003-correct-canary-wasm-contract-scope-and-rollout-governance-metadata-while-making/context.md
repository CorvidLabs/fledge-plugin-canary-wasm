---
change: CHG-0003-correct-canary-wasm-contract-scope-and-rollout-governance-metadata-while-making
artifact: context
---

# Context

The canonical Purpose and Public API claimed a separate persistence attack surface even though `src/main.rs` implements environment, filesystem, network, and process probes only. The rollout also omitted the CHG-0002 spec version and the four installed agent directories from meaningful path enforcement. Gemini's generated create-change guidance added a literal quote layer around an argument placeholder that can already arrive quoted. These corrections align governance with the current implementation without changing product code.
